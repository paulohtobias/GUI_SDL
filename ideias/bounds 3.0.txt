struct Bounds {
	Position global; //relative to the window.
	Position local; //relative to parent.
	...
}

struct Widget {
	...
	Camera *rendering_camera; //a pointer to the camera that is rendering the widget. Can be NULL (look for windows coordinates in this case).
	...
}

ScrollableContainer's camera field won't be a pointer anymore.
ScrollableContainer's camera's position are relative to window.

